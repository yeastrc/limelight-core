

build/libs contents

When build is triggered from building the web app,
'build/libs' will contain 2 files:

    limelight_submit_import.jar  - contains just the built java and resources from this project
    limelightSubmitImport.jar - runnable 'fat' jar with all the dependencies extracted and inserted into this jar.

