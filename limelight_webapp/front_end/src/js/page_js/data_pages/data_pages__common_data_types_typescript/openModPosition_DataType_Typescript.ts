/**
 * openModPosition_DataType_Typescript.ts
 *
 * Type declaration for open mod position since can be either a string or a number.
 *
 * Could create an Enum for the string but not done for now
 *
 */

/**
 * Type declaration for OpenModPosition since  can be either a string or a number.
 *
 * Could create an Enum for the string but not done for now
 *
 */
export type OpenModPosition_DataType = number | string