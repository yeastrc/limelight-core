

Currently   !!! NO !!!   Handlebars templates in use in Limelight


Handlebars Templates are precompiled with --amt so they can be imported using syntax:

	import bundle_variable = require("...")

Currently no way to have the output have the ES6 export statement


 Example Import (Not currently in code)


import Handlebars = require('handlebars/runtime');

import _dummy_template_template_bundle = require("../../../../../../handlebars_templates_precompiled/dummy_template/dummy_template_template-bundle.js" );
